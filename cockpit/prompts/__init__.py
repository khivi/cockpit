"""First-turn prompt templates (``*.txt``) for the spawn scenarios.

Loaded + rendered by :mod:`cockpit.lib.templates`; the value computation and
mode selection live in :mod:`cockpit.spawn`. Keep these files as static prose
with ``{placeholder}`` slots only — no logic.
"""
